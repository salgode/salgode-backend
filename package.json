{
  "name": "salgode-backend",
  "description": "SalgoDe Backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "preinstall": "node -e \"if (process.env.npm_execpath.indexOf('yarn') === -1) throw new Error('Prefer yarn: https://yarnpkg.com/en/docs/install')\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "private": true,
  "devDependencies": {
    "eslint": "^6.5.1",
    "eslint-config-airbnb-base": "^14.0.0"
  }
}
